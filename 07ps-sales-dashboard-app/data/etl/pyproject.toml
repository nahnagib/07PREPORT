[project]
name = "powerbi-sales-pipeline"
version = "0.1.0"
description = "Odoo sale.report to Power BI Excel model pipeline"
requires-python = ">=3.10"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = [".", "src"]
