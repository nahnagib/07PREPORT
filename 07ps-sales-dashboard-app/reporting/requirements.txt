pymysql==1.1.2
SQLAlchemy==2.0.43
pandas==2.3.2
numpy==2.3.3
scikit-learn==1.7.2
shap==0.48.0
statsmodels==0.14.5
matplotlib==3.10.6
python-docx==1.2.0
python-dotenv==1.1.1

# Prophet is best-effort on Windows (needs cmdstanpy's C++ toolchain).
# If `pip install prophet` fails here, the pipeline falls back to SARIMAX-only
# and states that in the report's methodology note -- see src/forecast.py.
prophet==1.3.0
